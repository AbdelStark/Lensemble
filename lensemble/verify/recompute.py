"""lensemble.verify.recompute — see docs/rfcs/RFC-0006. Stub scaffolded by #2."""

from __future__ import annotations

from typing import Any


def recompute_alignment(committed_weights: Any, probe: Any) -> Any:
    """Publicly recompute frame alignment from public probe + committed weights (RFC-0006 4). #62."""
    raise NotImplementedError(
        "lensemble.verify.recompute_alignment is implemented by #62"
    )
